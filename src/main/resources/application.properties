# configure username and password for spring security
# spring.security.user.name=isaccanedo
# spring.security.user.password=123456789

#-------------------------------------------------------
# - CONFIGURAÇÃO DO DATA SOURCE H2
#-------------------------------------------------------

spring.datasource.driverClassName = org.h2.Driver
spring.datasource.url = jdbc:h2:mem:bancoMemoria
spring.datasouce.username = sa
spring.datasouce.password = 

# JPA
spring.jpa.database-platform = org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto = create

# H2
spring.h2.console.enabled = true
spring.h2.console.path = /h2-console

jwt.signing.key.secret=mySecret
jwt.get.token.uri=/authenticate
jwt.refresh.token.uri=/refresh
jwt.http.request.header=Authorization
jwt.token.expiration.in.seconds=604800

spring.jpa.show-sql=true
spring.h2.console.enabled=true